
/** \brief Recibe los numeros y calcula la suma entre ellos
 *
 * \param n1 corresponde  al primer numero ingresado por el usuario.
 * \param n2 corresponde  al segundo numero ingresado por el usuario.
 * \return corresponde a la suma de los numeros ingresados por el usuario.
 *
 */
float sumar(float,float);

/** \brief Recibe los numeros ingresados por el usuario y calcula la resta entre ellos.
 *
 * \param n1 corresponde  al primer numero ingresado por el usuario.
 * \param n2 corresponde  al segundo numero ingresado por el usuario.
 * \return devuelve el resultado, la resta de los numeros.
 *
 */

float restar(float, float);

/** \brief Recibe los numeros ingresados por el usuario y calcula la division de ellos.
 *
 * \param n1 corresponde  al primer numero ingresado por el usuario.
 * \param n2 corresponde  al segundo numero ingresado por el usuario.
 * \return devuelve el resultado, la division de los numeros ingresados por el usuario.
 *
 */

float dividir(float,float);

/** \brief Recibe los numeros ingresados por el usuario y los multiplica.
 *
 * \param n1 corresponde  al primer numero ingresado por el usuario.
 * \param n2 corresponde  al segundo numero ingresado por el usuario.
 * \return Devuelve el producto de los numeros ingresados por el usuario.
 *
 */

float multiplicar(float,float);

/** \brief Recibe el numero imngresado por el usuario y lo multiplica por los anteriores.
 *
 * \param n1 corresponde  al primer numero ingresado por el usuario.
 * \return Devuelve el factorial del numero ingresado.
 *
 */

float flotante(float n1);



